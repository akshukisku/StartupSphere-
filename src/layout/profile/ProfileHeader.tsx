"use client";

import SectionHeader from "@/components/common/SectionHeader";

const ProfileHeader = () => {
  return (
    <SectionHeader
      title="My Profile"
      description="Manage your personal information, profile photo, and account details."
    />
  );
};

export default ProfileHeader;